- content_for :title do
     = "Haku :: #{@q}"
- content_for :content do
     %h2= "Hakusi oli: #{@q}"

     - if @result.empty?
          %p= "Hakusi ei tuottanut tuloksia"
     - else
          %ul
               - @result.each do |course|
                    %li= link_to course["name"] , course_exams_url(:course_id => course["course_id"])
