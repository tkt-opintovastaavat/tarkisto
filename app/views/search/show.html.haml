%h2= "Hakusi oli: #{@q}"

- if @result.empty?
     %p= "Hakusi ei tuottanut tuloksia"
- else
     %ul
          - @result.each do |course|
               %li= link_to course["name"] , exams_url(:course_id => course["course_id"])
