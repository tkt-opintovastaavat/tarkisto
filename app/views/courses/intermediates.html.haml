- content_for :title do
     = t 'courses.levels.intermediate'
- content_for :content do

     %p= link_to t('pages.courses.back'), root_url

     %table#intermediate_studies
          %tr
               %th= t 'courses.levels.intermediate'
          
          = render :partial => "courses", :locals => {:courses => @courses}