- content_for :title do
     = t 'courses.levels.advanced'
- content_for :content do

     %p#back= link_to t('pages.courses.back'), root_url
     
     %p#hide-button
          %a(href="#hideshow-noexams")=t 'pages.courses.show-hide-unpublished'

     %table#advanced_studies
          %tr
               %th= t 'courses.levels.advanced'
          
          = render :partial => "courses", :locals => {:courses => @courses}