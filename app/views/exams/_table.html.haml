- if exams.empty?
     %p Empty
- else
     %table
          - exams.each do |exam|
               %tr
                    %td= link_to exam.exam_type.name, course_exam_url(exam.course, exam)
                    %td= exam.date
                    %td= link_to "PDF", course_exam_url(exam.course, exam)

