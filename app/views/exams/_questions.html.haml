- if exam.nil?
     %p Empty
- else
     %p Maksimipisteet: #{points}
     - exam.each do |question|
          %h3 #{question.number}. #{question.name} <pre>#{question.description}</pre> Pisteet: #{question.points}
          %br
